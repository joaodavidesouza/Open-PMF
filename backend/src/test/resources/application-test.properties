# This is the key: Disables the Ryuk reaper container that conflicts with Fedora/SELinux
ryuk.disabled=true

# This prevents errors if the Docker client version is slightly different
check.client.version=false

# This explicitly tells Testcontainers where to find Docker, removing all ambiguity
docker.host=unix:///var/run/docker.sock
